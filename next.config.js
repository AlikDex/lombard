/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   serverActions: true,
  // },
  // distDir: "build",
  //output: "export",
  basePath: "",
  env: {
    // PUBLIC_URL: "/",
  },
};

module.exports = nextConfig;
